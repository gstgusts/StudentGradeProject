@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    @Model.TestValue
    <ol>
        @foreach (var student in Model.Students)
        {
            <li>@student.Name</li>
        }
    </ol>
</div>
